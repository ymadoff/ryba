[Unit]
Description=HDFS NameNode

[Service]
User={{hdfs.user.name}}
Group={{hdfs.group.name}}
PIDFile={{hdfs.pid_dir}}/hadoop-hdfs-namenode.pid
ExecStart=/usr/hdp/current/hadoop-hdfs-namenode/../hadoop/sbin/hadoop-daemon.sh --config {{hdfs.nn.conf_dir}} --script hdfs start namenode
ExecReload=/usr/hdp/current/hadoop-hdfs-namenode/../hadoop/sbin/hadoop-daemon.sh --config {{hdfs.nn.conf_dir}} --script hdfs restart namenode
ExecStop=/usr/hdp/current/hadoop-hdfs-namenode/../hadoop/sbin/hadoop-daemon.sh --config {{hdfs.nn.conf_dir}} --script hdfs stop namenode

[Install]
WantedBy=multi-user.target
